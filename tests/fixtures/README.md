# Small fixtures

Store small, inspectable, provenance-labelled fixtures here. The original BAM
examples remain in `.test/`. Do not commit downloaded human references or create
invented golden calls. Each future golden fixture must identify its baseline
commit, input hashes, parameters, and reference environment.
