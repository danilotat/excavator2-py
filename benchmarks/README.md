# Benchmarks

Add stage and kernel benchmarks after the oracle and first working algorithms.
Measure wall time, peak RSS, input size, thread count, CPU, compiler, and backend.
Compare against Python/NumPy before expanding custom C++. No speedup is claimed
by the build scaffold.
